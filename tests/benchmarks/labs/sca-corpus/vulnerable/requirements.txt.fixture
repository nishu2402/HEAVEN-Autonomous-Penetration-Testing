# HEAVEN SCA benchmark corpus — deliberately VULNERABLE Python pins.
#
# Every version below is a real, long-published release with permanent OSV/CVE
# advisories whose affected range includes the exact pinned version. The scorer
# (tests/benchmarks/sca_benchmark.py) audits this file live against OSV.dev and
# asserts the curated must-find CVEs in ground_truth/sca.yaml are all detected.
# These are inert manifest entries: nothing is installed or executed.
Django==2.2.0
urllib3==1.24.1
requests==2.19.1
PyYAML==5.1
Jinja2==2.10
