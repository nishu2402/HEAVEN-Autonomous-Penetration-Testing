# Every file in this directory is transient per-run benchmark output,
# regenerated by `heaven benchmark` / `pytest tests/benchmarks/`. None of it is
# tracked. The committed, human-readable benchmark evidence lives in
# docs/BENCHMARK_RESULTS.md (reproduce with docs/BENCHMARK_HOWTO.md).
*
!.gitignore
