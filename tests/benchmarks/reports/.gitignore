# Transient per-run benchmark artifacts (regenerated by `pytest tests/benchmarks/`).
# The curated baseline (dvwa_live.md) is committed deliberately; the rest is
# run output and must not be tracked.
*
!.gitignore
!dvwa_live.md
